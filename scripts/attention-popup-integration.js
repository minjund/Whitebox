'use strict';

// Keep the historical CI entry point while asserting its replacement: the
// exact owning PTY opens in the full-screen focus surface and old popup DOM is
// absent.
require('./drawer-terminal-visual');
